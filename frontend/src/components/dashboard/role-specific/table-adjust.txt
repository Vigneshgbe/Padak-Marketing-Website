ALTER TABLE contact_messages
ADD COLUMN status ENUM('pending', 'contacted', 'resolved', 'closed') NOT NULL DEFAULT 'pending';

npm install react-hot-toast

npm install chart.js react-chartjs-2

ALTER TABLE courses ADD COLUMN image_url VARCHAR(255) DEFAULT NULL AFTER description;